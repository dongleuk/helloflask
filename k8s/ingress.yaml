apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flask-demo-ingress
spec:
  rules:
  - host: flask.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello-flask
            port:
              number: 80